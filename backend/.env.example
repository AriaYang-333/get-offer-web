# 复制本文件为 .env 并填入你自己的值（.env 不会被上传到仓库）
# ===== 上线前必须改 =====
# 管理员密钥：改成只有你知道的强随机串（可用命令生成：openssl rand -hex 16）
ADMIN_KEY=改成你自己的强随机串-例如openssl-rand-hex-16的结果

# 监听端口（云平台会自动注入，本地默认 3000，一般不用改）
PORT=3000

# ===== 可选 =====
# 静态目录（放产品页 index.html 的地方），默认是 backend 的上级目录
# STATIC_DIR=/path/to/static

# 反馈转发 Webhook（企业微信/飞书/Slack 机器人，填了就把每条反馈 POST 过去）
# FEEDBACK_WEBHOOK=

# 反馈抄送邮箱（仅展示，不实际发信）
# FEEDBACK_EMAIL=
